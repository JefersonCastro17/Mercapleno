services:
  db:
    image: postgres:16-alpine
    container_name: mercapleno-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: mercapleno
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
<<<<<<< HEAD
      test: ["CMD-SHELL", "pg_isready -U postgres -d mercapleno"]
=======
      test:
        [
          "CMD-SHELL",
          "mariadb-admin ping -h 127.0.0.1 -uroot -proot123 --silent || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
    restart: unless-stopped

  test-db:
    image: mariadb:10.11
    environment:
      MARIADB_ROOT_PASSWORD: root123
      MARIADB_DATABASE: mercapleno_test
    ports:
      - "3308:3306"
    volumes:
      - mysql_test_data:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mariadb-admin ping -h 127.0.0.1 -uroot -proot123 --silent || exit 1",
        ]
>>>>>>> origin/feature/pruebas-unitarias-auth-admin-user
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  test-db:
    image: postgres:16-alpine
    container_name: mercapleno-postgres-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: mercapleno_test
    ports:
      - "5433:5432"
    volumes:
      - pg_test_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d mercapleno_test"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  backend:
    build:
      context: ../mercapleno-backend
      dockerfile: Dockerfile.dev
    environment:
      PORT: 4000
      DATABASE_URL: postgresql://postgres:postgres123@db:5432/mercapleno?schema=public
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres123
      DB_NAME: mercapleno
      JWT_SECRET: yogui
      JWT_EXPIRES_IN: 2h
      ADMIN_ROLE_ID: 1
      SMTP_HOST: smtp.gmail.com
      SMTP_PORT: 587
      SMTP_SECURE: false
      SMTP_USER: mercapleno@gmail.com
      SMTP_PASS: bjco veem vzie ubda
      SMTP_FROM_EMAIL: mercapleno@gmail.com
      APP_NAME: mercapleno
      EMAIL_VERIFICATION_TTL_MIN: 15
      LOGIN_2FA_TTL_MIN: 10
      PASSWORD_RESET_TTL_MIN: 15
      INTERNAL_API_KEY: mercapleno123456789
      LOW_STOCK_THRESHOLD: 5
      CORS_ORIGINS: http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "4000:4000"
    restart: unless-stopped

  frontend:
    build:
      context: ../Propio
      dockerfile: Dockerfile.dev
    depends_on:
      - backend
    ports:
      - "5173:5173"
    restart: unless-stopped

volumes:
<<<<<<< HEAD
  pg_data:
  pg_test_data:
=======
  mysql_data:
  mysql_test_data:
>>>>>>> origin/feature/pruebas-unitarias-auth-admin-user
